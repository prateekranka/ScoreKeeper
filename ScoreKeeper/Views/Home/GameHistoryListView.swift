import SwiftUI

// The production GameHistoryListView lives in Views/Stats/GameHistoryListView.swift.
// This compatibility source remains because the Xcode project still contains the
// historical file reference. Keeping it intentionally type-free avoids duplicate
// declarations while preserving deterministic project builds.
